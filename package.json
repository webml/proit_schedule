{
  "name": "proit_shedule",
  "homepage": "https://webml.github.io/proit_schedule/",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rsbuild dev --open",
    "deploy": "gh-pages -d master -d dist",
    "preview": "rsbuild preview",
    "build": "rsbuild build"
  },
  "dependencies": {
    "gh-pages": "^6.1.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "rsuite": "^5.52.0"
  },
  "devDependencies": {
    "@rsbuild/core": "^0.3.9",
    "@rsbuild/plugin-react": "^0.3.9"
  }
}
